package common

val wordToFind = "XMAS"
val wordSize = wordToFind.length

fun countOccurrence(input: List<List<Char>>): Int {
	
	var counter = 0
	
	input.forEach { line ->
	
	}
	
	return counter
}

fun getHorizontal(line: List<Char>): Int {
}
